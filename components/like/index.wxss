/* components/like/index.wxss */

.container{
  display: inline-flex; /*使container的宽度自适应*/
  flex-direction: row;
  padding: 10rpx;
}

.container image{
  /* 使用px的话需要除以2(如果是以iPhone6的尺寸来设置的话) */
  /* 绝大多数情况都可以选择使用rpx(resilient px?) */
  width: 32rpx;
  height: 28rpx;
}

.container text{
  font-size: 24rpx;
  color: #bbb;
  position: relative;
  bottom: 10rpx;
  left: 6rpx;
  line-height: 24rpx;
}